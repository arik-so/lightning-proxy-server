import * as chai from 'chai';

const assert = chai.assert;

describe('Web Client Integration Test', () => {

	it('should complete a full setup round with the server', () => {
		assert.equal(2 + 2, 4);
	});

});
